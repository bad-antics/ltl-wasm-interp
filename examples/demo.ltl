// examples/demo.ltl — runnable demo for ltl_wasm_interp

import std::io
use ltl_wasm_interp as lib

fn main() {
    io::println("ltl_wasm_interp demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}